/**
 * BAD EXAMPLE: Hardcoded text transformations in subclasses.
 *
 * Each combination of formatting needs its own class.
 */
public class TextProcessor {

    public String process(String text) {
        return text;
    }
}
